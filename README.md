# Banking-Project
Implementation of a Banking System using MySQL, Flask and React.

Create a python virtual environment and run pip install -r requirements.txt
Run schema.sql in MySQL server to create the bank database and customer, account, transactions table.
